# Billing — Matriz de contradicciones entre documentos

Auditoría: arquitecto principal. Fecha: 2026-07-22. Alcance: los 16 docs de `documents/architecture/billing/`.
Método: cada fila cita el texto real de dos documentos (o de un documento y el código) que se contradicen.

| ID | Doc A | Doc B | Afirmación A | Afirmación B | Impacto | Decisión recomendada |
|---|---|---|---|---|---|---|
| C-01 | `08_State_Machines.md:34` | `06_Domain_Design.md:74` (enum `InvoiceStatus`) | "reembolso total → invoice queda **Paid** con AmountPaid reducido" | `InvoiceStatus { Draft, Issued, Sent, PartiallyPaid, Paid, Voided }` — **no existe `Refunded`** | Una factura 100% reembolsada reporta `Paid` con `AmountPaid=0`. Reportes/contabilidad/impuestos incorrectos. Corrupción fiscal. | Agregar estado `Refunded` (y `PartiallyRefunded` opcional) a `InvoiceStatus`; definir que `RegisterRefund` total → `Refunded`, parcial → `PartiallyRefunded`. NO reabrir `AmountDue`. Ver `04_Domain_Recommendations`. |
| C-02 | `08_State_Machines.md:33` | `06_Domain_Design.md:32` | "PartiallyPaid (saldo **reabierto** por reembolso parcial)" | "RegisterRefund … reduce `AmountPaid`; marca recibo `Refunded`" | Un reembolso reduce `AmountPaid` → `AmountDue` sube → la factura parece que "se debe otra vez". Semánticamente falso; el cliente no debe dinero tras un reembolso. | Un reembolso NO reabre saldo cobrable. Separar `AmountRefunded` de `AmountDue`. Estado `PartiallyRefunded`/`Refunded`, no `PartiallyPaid`. |
| C-03 | `06_Domain_Design.md:30` / `08:57` (idempotente por `PaymentReference`) | `09_Commands_And_Events.md` / `13_Payment_Prerequisite_Issues.md` (correlación por `(PaymentSource, PaymentId)`) | "RecordPayment es idempotente por `PaymentReference`" | Los eventos `payments.*` se correlacionan por `(PaymentSource, PaymentId)` | ¿La clave de deduplicación es `PaymentReference` (string, ¿de dónde?) o `PaymentId`? El pago **manual** no tiene `PaymentId`. Doble emisión de recibo o pago perdido. | Definir clave canónica: online = `(PaymentSource, PaymentId)`; manual = `Idempotency-Key` del request. `PaymentReference` es dato, no clave. |
| C-04 | `07_Use_Case_Catalog.md` (UC-04 render PDF; UC-05 render watermark) / `08:47` | `13_Payment_Prerequisite_Issues.md` (BDR-003) / `15_Implementation_Plan.md` (B5) | "UC-04 Send: Render PDF (Scribe→CloudStorage)"; "RecordPayment … PDF watermark" | "invoice válida sin PDF; PDF en B5"; "fallback: emitir sin PDF" | ¿El PDF se crea al enviar (07/08) o se difiere (13/15)? Un `send` que exige PDF pero el PDF no existe hasta B5 es incoherente. | El envío NO debe bloquear por PDF. `PdfFileId` nullable; render asíncrono con `InvoiceDeliveryStatus`. Corregir 07/08 para decir "render best-effort". |
| C-05 | `01_Executive_Summary.md:4` (`READY_FOR_SCAFFOLDING`) | `13` (BDR-001..004) + `16_Open_Questions.md` (OQ-01 tax, OQ-04 partial) | "Estado final: `READY_FOR_SCAFFOLDING`" | Decisiones de producto (impuestos, alcance de pago parcial) y gaps de dominio (refund state, sobrepago, pago-tras-void) sin resolver | El estado global oculta que scaffolding≠implementación. Implementar sobre decisiones abiertas produce retrabajo. | Mantener `READY_FOR_SCAFFOLDING` SOLO para scaffolding; marcar `NOT_READY_FOR_IMPLEMENTATION` hasta cerrar C-01/03/07 y OQ-01/04. Ver `11_Readiness_Scorecard`. |
| C-06 | `06_Domain_Design.md:30` (`Invoice.RecordPayment` **crea y devuelve** `PaymentReceipt`) | `05_Ownership_Matrix.md` (Invoice y PaymentReceipt son **aggregate roots** distintos) | "RecordPayment … crea y devuelve el `PaymentReceipt`" | Ambos son aggregate roots (consistencia individual) | Un aggregate root creando otro aggregate root en su método viola el límite transaccional recomendado (una mutación de escritura por aggregate por transacción). | Alternativa A: Invoice muta saldo + emite `InvoicePaid`; el **handler/application service** crea el `PaymentReceipt`. Ver `04_Domain_Recommendations`. |
| C-07 | `10_Data_Model.md` / `06` (Billing recibe montos precalculados y los congela) | Buenas prácticas financieras + prompt de auditoría ("no aceptes el total del frontend como verdad") | "Billing acepta montos precalculados por el caller … los congela como snapshot" | El caller (frontend) controla `Total`/`TotalTax` → manipulable | Un caller malicioso factura $0 de impuesto o total arbitrario. Riesgo de fraude/manipulación fiscal. | MVP: Billing recibe **componentes** (cantidad, precio unitario, tasa, descuento) y **recalcula server-side** subtotal/impuesto/total. Congela el resultado recalculado, no el del caller. Ver `04`. |
| C-08 | `13` (BDR-004: cancelación de link **best-effort**, no bloquea void) | `07` (UC-09) / `08:46` ("cancela cobro pendiente en PaymentClient") | "Best-effort … si falla, se registra y reintenta" | "Void … cancela cobro pendiente en PaymentClient" (implica garantizado) | Si la cancelación falla, el link de pago sigue vivo sobre una factura anulada → el cliente paga una factura `Voided` → pago huérfano (ver C-09). | Void debe (a) marcar link `PendingCancel`, (b) reintentar cancelación con outbox, (c) si llega un pago sobre factura `Voided`, disparar reembolso automático + alerta. No "best-effort silencioso". |
| C-09 | `08:56` (`RecordPayment` sobre `Voided` → `NotPayable`) | (ausencia) — ningún doc define "pago recibido después de anular" | "No se puede `RecordPayment` sobre Draft/Issued/**Voided** → `NotPayable`" | El prompt pide definir "pago recibido después de anular"; los docs no lo hacen | El evento `payments.payment_succeeded` llega tras el void → `NotPayable` → el dinero YA fue cobrado por PaymentClient → pago huérfano sin recibo. | Definir explícitamente: pago sobre `Voided` ⇒ registrar el hecho, disparar reembolso automático (`RegisterRefund`) + evento `billing.invoice.payment_after_void` + alerta. Nunca descartar en silencio. |
| C-10 | `08:47-48` (`RecordPayment` requiere `amount == AmountDue` o `0 < amount < AmountDue`) | (ausencia) — ningún doc cubre `amount > AmountDue` | Transiciones solo para pago igual o menor al saldo | El prompt pide "pago mayor que el saldo" | Sobrepago sin manejo → `RecordPayment` sin transición válida → excepción o estado corrupto. | Definir: `amount > AmountDue` ⇒ rechazar (`Billing.Invoice.Overpayment`) o registrar crédito. MVP: rechazar y alertar; documentar. |
| C-11 | `08:46` (`Void` de `PartiallyPaid` → `AmountDue=0`) | `06:32` (dinero ya pagado vive en `AmountPaid`) | "Void … `AmountDue=0`" | Una `PartiallyPaid` tiene `AmountPaid > 0` | Anular una factura con dinero ya cobrado pone `AmountDue=0` pero ignora el `AmountPaid`; el cliente pagó y la factura se anula sin reembolso. | Void de `PartiallyPaid`/con `AmountPaid>0` debe exigir/disparar reembolso del monto pagado antes de `Voided`, o prohibirse. |
| C-12 | `11_API_Contracts.md` (`POST /billing/invoices/{id}/mark-as-paid`) | `06`/`08` (soporta pago **parcial**) | "mark-as-paid" (semántica de pago total) | La factura soporta `PartiallyPaid` | "mark-as-paid" implica saldar; con pagos parciales el nombre miente. | Renombrar a `POST /billing/invoices/{id}/payments/manual` (recurso de pago, permite parcial). |
| C-13 | `06:45` (hash sobre `…AmountCents…`) + `ValueObjects/Money` (`AmountCents`) | prompt ("monedas ISO-4217 con cero, dos o tres decimales") | Todo monto en `AmountCents` (asume 2 decimales) | Existen monedas de 0 (JPY) y 3 (BHD, KWD) decimales | El nombre y la aritmética `Cents` asumen minor-unit=2. Para JPY/BHD el redondeo y la representación son incorrectos. | Renombrar a `MinorUnits` + tabla de exponente por moneda (ISO-4217), o restringir explícitamente a monedas de 2 decimales en el MVP y documentarlo como límite. |
| C-14 | `09_Commands_And_Events.md` (`billing.invoice.sent` lleva `CustomerEmail`) | `05`/`principio de mínima exposición` | Evento con `CustomerEmail` (PII) al exchange compartido `taxvision-events` | Todos los servicios ven el fan-out | PII (email) circula en el bus a consumidores que no la necesitan. Sobre-exposición. | El evento lleva `InvoiceId`/`CustomerId`; Notification resuelve el email vía Customer M2M. O cifrar/minimizar. Ver `06_Security_Review`. |

## Contradicciones priorizadas por el prompt (resumen)

- **Reembolso total / estado tras refund** → C-01, C-02 (Critical).
- **Quién calcula impuestos** → C-07 (High, decisión de producto).
- **Creación del PaymentReceipt** → C-06 (High).
- **Momento de creación del PDF** → C-04 (Medium).
- **Cuándo pasa a Sent** → cubierto sin contradicción grave (07/08 coinciden); el matiz es el PDF (C-04).
- **Cancelación best-effort** → C-08 (High).
- **Alcance de pagos parciales** → C-10, C-11, C-12 (High/Medium).
- **READY_FOR_SCAFFOLDING vs blockers** → C-05 (High).

## Documentos originales a corregir

| Documento | Corrección requerida |
|---|---|
| `06_Invoices_Domain_Design.md` | Enum `InvoiceStatus` + `Refunded`/`PartiallyRefunded`; separar `AmountRefunded`; mover creación de `PaymentReceipt` fuera de `RecordPayment` (Alt. A); recibir componentes y recalcular. |
| `08_Invoices_State_Machines.md` | Líneas 33-34, 46-50: estados de refund, void de PartiallyPaid, sobrepago, pago-tras-void. |
| `07_Invoices_Use_Case_Catalog.md` | UC-04/05: PDF best-effort; UC-05 renombrar endpoint; UC-06/08 idempotencia canónica. |
| `09_Billing_Commands_And_Events.md` | Clave de idempotencia; PII en `sent`/`receipt.issued`. |
| `10_Billing_Data_Model.md` | `MinorUnits` vs `Cents`; `AmountRefunded`; `InvoicePaymentLinks` con `TenantId/ExpectedAmount/Currency/PurposeKind`. |
| `11_Billing_API_Contracts.md` | `mark-as-paid`→`payments/manual`; ETag/If-Match; versionado `/v1`. |
| `01_Executive_Summary.md` | Aclarar que el estado aplica solo a scaffolding. |
