# PayFlow — 02. Comparison Matrix (actual vs nuevo)

Matriz de referencia para las Fases 3-19. Cada fila corresponde a uno de los 40 pasos de `01_FlowSpec.md`. Cada cita `archivo:línea` fue verificada abriendo el archivo real el 2026-07-28; las que cambiaron de ubicación desde el análisis original quedan corregidas y marcadas explícitamente.

## Correcciones de paths respecto a citas previas del plan

- **Subscription `TenantCreatedConsumer`**: el plan (Anexo D) lo cita en `TaxVision.Subscription.Application/Tenants/IntegrationEvents/TenantCreatedConsumer.cs` — **path incorrecto, no existe**. Ubicación real verificada: `src/Services/Subscription/TaxVision.Subscription.Application/Subscriptions/IntegrationEvents/TenantCreatedConsumer.cs`. El método `CreateTrialSubscriptionOrThrow` sí está en la línea 82 tal como citado.
- Todas las demás citas de Anexo D verificadas (`ReserveSubdomainHandler`, `PasswordCommands.cs`, `GenerateTenantRegistrationTicket`, `SaaSPayment.cs`, `UX_SaaSPayments_IdempotencyKey`, `UX_WebhookEvents_ProviderCode_ProviderEventId`, `DocumentGeneration.cs`, `billing.invoice.v1`, `Tenant.cs`, `TenantSubscription.cs`, `Invoice.cs`, `RenderController.cs`, `NotificationsEmailSendRequestedConsumer.cs`) **coinciden con el estado actual del repo** — sin cambios necesarios.
- Ningún endpoint M2M nuevo del flujo PayFlow existe todavía (`/tenants/internal/from-onboarding`, `/subscriptions/internal/activate-from-onboarding`, `/internal/document-generations/onboarding-receipts`) — confirmado por grep, cero matches. Esperado: aún no se ha implementado ninguna fase de código.

---

## Matriz completa (40 filas)

| # | Existe hoy | Servicio hoy | Servicio recomendado | Eventos actuales | Eventos necesarios | Tablas nuevas/cambios | Impacto | Riesgo | Decisión | Fase |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | Sí (parcial) | Subscription (`GET /plans`, `PlansController.cs:15`) | Subscription (sin cambios) + Auth.Onboarding recibe `PlanId` | Ninguno | Ninguno | Ninguna | Bajo | Elegir plan desactivado entre selección y pago | Reusar `GET /plans`; validar `Plan.IsActive` en el checkout, no solo en frontend | Fase 8 |
| 2 | No | — | Auth.Onboarding | Ninguno | Ninguno | Ninguna directa | Bajo | Bajo | Combinar con paso 3 en un solo endpoint | Fase 5 |
| 3 | Parcial (patrón MFA OTP análogo en Auth) | Auth (Mfa) | Auth.Onboarding (nuevo agregado) | `MfaChallengeRequestedIntegrationEvent` (análogo, no reusable directo) | Ninguno (challenge síncrono) | `EmailVerificationChallenges` | Medio | Abuso/spam de OTP sin rate limiting | Nuevo agregado `EmailVerificationChallenge` + `IOnboardingOtpThrottler` (Redis) | Fase 5 |
| 4 | Parcial (patrón de verificación MFA existe) | Auth (Mfa) | Auth.Onboarding | Ninguno | Ninguno | (mismo agregado del paso 3) | Bajo | Bajo (rate limit de intentos) | `VerifyEmailChallenge` command | Fase 5 |
| 5 | No | — | Auth.Onboarding | Ninguno | Ninguno | Campos en `TenantOnboarding` (FirstName/LastName/Phone) | Bajo | Bajo | Combinar con creación de `TenantOnboarding` (paso 6) | Fase 4 |
| 6 | No | — | Auth.Onboarding (nuevo aggregate) | Ninguno | Ninguno | `TenantOnboardings` | Alto | Diseño de estados/guards debe ser correcto desde el inicio | Nuevo aggregate con 12 estados + 8 steps | Fase 4 |
| 7 | No | — | Auth.Onboarding | Ninguno | Ninguno | (mismo `TenantOnboardings`) | Alto | No enviar a Stripe si no persistió antes (regla de consistencia) | Transacción EF explícita antes de llamar a PaymentApp | Fase 4 |
| 8 | Parcial (infra Stripe reusable, `StripePaymentAdapter.cs:19,189,328`) | PaymentApp | PaymentApp (nuevo endpoint M2M) | Ninguno específico de onboarding | Ninguno en este paso (respuesta M2M síncrona) | Extender `SaaSPayment` con `SaaSPaymentType.OnboardingInitial` + `OnboardingId` nullable + IX filtrado | Medio | Permitir `TenantId=Guid.Empty` solo para este tipo — constraint de integridad | Extender `SaaSPayment`, NO crear agregado nuevo (§3.3 del plan) | Fase 8 |
| 9 | No | — | Auth.Onboarding (actualiza su propio agregado) | Ninguno | Ninguno | `TenantOnboardings.PaymentId/PaymentStatus` (cubierto en 6) | Bajo | Si esta UoW falla, debe quedar recuperable por `CheckoutReference+OnboardingId` | UoW inmediata tras respuesta M2M de PaymentApp | Fase 8 |
| 10 | N/A (Stripe, externo) | Externo | Externo (Stripe) | N/A | N/A | N/A | N/A | N/A | Sin cambios — paso externo al monorepo | N/A |
| 11 | Sí | PaymentApp (`StripeWebhookController.cs:22`, `ProcessStripeWebhookHandler.cs:65,112`) | PaymentApp (mismo, agrega branch por `OnboardingInitial`) | Eventos de pago tenant→cliente existentes | `OnboardingPaymentSucceededIntegrationEvent` (nuevo) | Ninguna nueva (reusa `WebhookEvents`+`SaaSPayments`) | Bajo (reusa infra existente) | Bajo — Inbox+idempotencia ya probados en producción | Branch condicional en handler existente | Fase 8/9 |
| 12 | Parcial (Inbox+Outbox existen; falta transición de `TenantOnboarding`) | PaymentApp (su parte) | PaymentApp (UoW #2) + Auth.Onboarding (consumer) | Ninguno de onboarding | `OnboardingPaymentSucceededIntegrationEvent` | `SaaSPayments.Status` (existente) + `TenantOnboardings.Status/PaymentCompletedAt` (nueva) | Alto — punto de no retorno del negocio | Doble cobro o pago no reflejado si la idempotencia falla — mitigado por Inbox ya probado | Dos UoW separadas coordinadas por evento, NO transacción distribuida (ver hueco §f.2 de 01_FlowSpec.md) | Fase 9 |
| 13 | N/A (regla de negocio, no paso ejecutable) | — | Auth.Onboarding + PaymentApp (regla transversal) | N/A | N/A | N/A | Alto (regla dura de negocio) | Violarla rompe la confianza del cliente | Documentada y aplicada vía §3.7 (cero refund automático) + `FailureClassifier` | Fase 17 |
| 14 | Parcial (patrón de tokens opacos hasheados existe: `Invitation.RawToken`, `PasswordResetToken`) | Auth (patrón existente) | Auth.Onboarding (nuevo `RegistrationToken` VO) | Ninguno | `OnboardingRegistrationReadyIntegrationEvent` (con `TokenReference`, no el raw — §3.6) | `TenantOnboardings.RegistrationTokenHash/ExpiresAt/UsedAt` (cubierto en 6) | Medio | Alto si el raw token se filtra por RabbitMQ — mitigado por `TokenReference`+Redis TTL 30s | `SecureTokenService` (32 bytes CSRNG + SHA256) + `RedisTokenReferenceStore` | Fase 9 |
| 15 | Sí (Outbox+retry ya existen) | Notification + Postmaster | Mismos, sin cambios de infraestructura | Patrón Outbox genérico ya usado | Ninguno nuevo | Ninguna | Nulo | Bajo — infraestructura ya probada | Sin cambios | N/A |
| 16 | No | — | Scribe (template) + Notification (consumer) + Postmaster (envío, sin cambios) | Ninguno | Consumo de `OnboardingRegistrationReadyIntegrationEvent` | Seed de template `onboarding.registration_ready` en Scribe | Bajo (sigue patrón de los 13 templates ya migrados) | Bajo | Nuevo consumer `OnboardingRegistrationReadyConsumer` | Fase 12 |
| 17 | No | — | Auth.Onboarding | Ninguno | Ninguno | Ninguna nueva | Medio | Exponer `OnboardingId` si se implementa mal — mitigado por diseño explícito | `CompleteTenantOnboarding` use case resuelve `token→hash→TenantOnboarding` internamente | Fase 13 |
| 18 | No | — | Auth.Onboarding | Ninguno | Ninguno | Ninguna | Bajo | Bajo | `GET /onboarding/register/{token}/preview` con datos enmascarados | Fase 13 |
| 19 | Parcial (`TenantSubdomainReservation`, TTL 15min, patrón análogo) | Auth (`TenantDomains`) | Auth.Onboarding (`OnboardingSubdomainReservation`, TTL 60min) | `TenantDomainReservedIntegrationEvent` (patrón análogo) | Ninguno obligatorio (validación síncrona) | `OnboardingSubdomainReservations` | Medio | Colisión de subdominio entre onboardings concurrentes — mitigado por soft-lock con TTL (ver hueco §f.3) | Nuevo agregado análogo a `TenantSubdomainReservation` pero scoped a Onboarding | Fase 14 |
| 20 | Parcial (`TenantTermsAcceptance` existe pero requiere `TenantId`; `TermsOptions.CurrentVersion` hardcodeado en `TermsOptions.cs:8`) | Auth (`Terms`) | Auth (retrofit `TenantTermsAcceptance` + nuevo `TermsVersion`) | `TenantTermsAcceptedIntegrationEvent` (existente, reusable) | Ninguno nuevo (mismo evento, ampliado con `TermsVersionId`) | `TermsVersions` + columnas `TermsVersionId+ContentHash` en `TenantTermsAcceptance` | Medio | Legal — versión de términos debe ser inmutable y auditable (IP+UserAgent) | Nuevo agregado `TermsVersion` + retrofit del existente | Fase 6 |
| 21 | No | — | Auth.Onboarding | Ninguno | Ninguno | Ninguna nueva | Bajo | Bajo (validación de shape del DTO) | `CompleteTenantOnboardingRequest` DTO explícito, sin campos de servidor | Fase 13 |
| 22 | No | — | Auth.Onboarding | Ninguno | Ninguno | Ninguna | Alto (gate crítico antes de Saga) | Saltarse una validación permite provisioning con datos corruptos | Cadena de guards explícitos en el aggregate | Fase 13 |
| 23 | No | — | Auth.Onboarding | Ninguno (transición interna) | Ninguno | Ninguna nueva | Alto | Bajo si el guard previo (paso 22) es correcto | `StartProvisioning` command inicia el Wolverine Saga | Fase 15 |
| 24 | N/A (principio arquitectónico) | — | Auth.Onboarding (Saga) | N/A | N/A | N/A | Alto (define arquitectura de Fase 15) | Alto si se ignora — es el error ya corregido al inicio de esta sesión (ver historial) | Wolverine Saga con `CorrelationId=TenantOnboardingId` | Fase 15 |
| 25 | Sí (parcial) — `POST /tenants` existe, no idempotente por `OnboardingId` ni M2M | Tenant (`TenantController.cs:44`) | Tenant (nuevo endpoint M2M, path viejo intacto) | `TenantCreatedIntegrationEvent` (existente, 5 consumidores actuales) | `TenantCreatedForOnboardingIntegrationEvent` (nuevo, o branch del existente con `OnboardingId`) | `AddOnboardingIdToTenants` (columna + IX filtrado único) | Medio | No romper los 5 consumidores actuales de `TenantCreatedIntegrationEvent` | Nuevo endpoint M2M `POST /tenants/internal/from-onboarding`, idempotente por `OnboardingId` | Fase 16 |
| 26 | No | — | Auth.Onboarding (Saga) | Consume el evento del paso 25 | Ninguno | `TenantOnboardings.TenantId` (columna cubierta) | Alto | Bajo si Wolverine Saga está bien configurado | Saga handler | Fase 15 |
| 27 | Parcial (Auth ya crea `User`+roles en invitaciones, sin M2M síncrono para esto) | Auth | Auth (comando local, mismo servicio que hostea la Saga) | `UserRegisteredIntegrationEvent` (patrón existente) | `TenantOwnerCreatedIntegrationEvent` (nuevo) | Ninguna nueva (reusa `Users`) | Medio | Password en tránsito — mitigado por §3.5 (nunca en RabbitMQ, mismo proceso) | Comando local `CreateTenantOwner` (no HTTP — mismo servicio) | Fase 15 |
| 28 | N/A (regla de seguridad, no paso ejecutable) | — | Auth | N/A | N/A | N/A | Alto (seguridad) | Alto si se viola — mitigado porque la Saga vive en el mismo servicio que crea el User (§3.1) | Hash inmediato en primera línea del handler, `[JsonIgnore]` en campo temporal del Saga | Fase 15 |
| 29 | Sí (parcial) — `POST /subscriptions/activate` existe, arranca `Trialing`, no M2M ni idempotente | Subscription (`SubscriptionsController.cs:86`) | Subscription (nuevo endpoint M2M) | Ninguno de onboarding | `SubscriptionActivatedForOnboardingIntegrationEvent` | `AddOnboardingIdToTenantSubscriptions` | Medio | Doble-creación de subscription si no es idempotente | Nuevo endpoint M2M `POST /subscriptions/internal/activate-from-onboarding`, arranca `Active` no `Trialing` | Fase 16 |
| 30 | No | — | Auth.Onboarding (Saga) + Subscription | Ninguno nuevo (mismo canal de fallos de la Saga) | Ninguno | `TenantOnboardings.FailedStep/FailureCode/FailureReason` (cubiertas en 6) | Alto | Cliente bloqueado sin poder avanzar — mitigado por `FailureClassifier`+retry+ManualReview | `ProvisioningFailed` + retry automático vía Polly + escalamiento a `ManualReview` | Fase 17 |
| 31 | Parcial (patrón de idempotencia por `IdempotencyKey` ya existe en `SaaSPayment` y `Postmaster.SqlIdempotencyGuard`) | Postmaster, PaymentApp (referencia) | Tenant, Auth, Subscription, CloudStorage (todos los pasos de provisioning) | N/A (principio transversal) | N/A | `OnboardingId` como columna con IX único filtrado en cada tabla de recurso creado | Alto | Sin esto, un retry duplica recursos | `OnboardingId` como clave de idempotencia en cada endpoint M2M nuevo | Fases 15/16 |
| 32 | Parcial (CloudStorage ya provisiona buckets/folders por tenant; Auth ya activa subdominio en Path A) | CloudStorage + Auth (`TenantDomains`) | Mismos, orquestados por la Saga en orden CloudStorage→Subdomain→Defaults (§6.2) | Patrones existentes de provisioning de storage | `StorageProvisionedIntegrationEvent`, `SubdomainActivatedIntegrationEvent`, `DefaultsConfiguredIntegrationEvent` (o comandos locales/M2M) | Ninguna nueva obligatoria (reusa infra existente) | Medio | Orden incorrecto entre pasos — mitigado porque el orden ya está fijado en `TenantProvisioningStep` (§6.2) | Reusar infraestructura existente de CloudStorage y TenantDomain, coordinada por la Saga | Fase 15 |
| 33 | No | — | Auth.Onboarding | Ninguno | `TenantOnboardingCompletedIntegrationEvent` | `TenantOnboardings.Status/RegistrationTokenUsedAt/RegistrationCompletedAt` (cubiertas en 6) | Alto (marca el fin del proceso) | Marcar Completed antes de que todos los pasos terminen — mitigado por guard explícito de todos los `TenantProvisioningStep` | Última UoW, solo si todos los steps obligatorios respondieron | Fase 15 |
| 34 | No — decisión de diseño explícita, distinta del patrón ingenuo | — | Auth.Onboarding | Ninguno nuevo | Ninguno | `RegistrationTokenUsedAt` (cubierta en 6) | Alto — si se consume antes, el cliente queda bloqueado ante cualquier fallo | Concurrencia — dos requests simultáneos con el mismo token deben bloquear el segundo | Guard `RegistrationTokenUsedAt IS NULL` + lock optimista (concurrency token) | Fase 13 (validación) + Fase 15 (consumo) |
| 35 | No | — | Auth.Onboarding | Ninguno | Ninguno | Ninguna nueva | Medio (UX) | Frontend bloqueado esperando indefinidamente (ver hueco §f.4 de 01_FlowSpec.md) | `GET /onboarding/status?token=...` con polling, `202 Accepted` inicial | Fase 13 |
| 36 | No | — | Auth.Onboarding (API) + Frontend | Ninguno | Ninguno | Ninguna | Medio (UX, no lógica de negocio) | Mensaje genérico de error asusta al cliente que ya pagó | Código de estado + mensaje distinto para `ProvisioningFailed`/`ManualReview` vs error genérico | Fase 13/19 |
| 37 | No | — | Auth.Onboarding (Domain) | N/A | N/A | Columnas `Status` (12 valores) + `CurrentStep` (8 valores) en `TenantOnboardings` | Alto (define toda la máquina de estados) | Bajo si se implementa fielmente según §6.1/6.2 del plan | Enums `TenantOnboardingStatus` + `TenantProvisioningStep` exactos del PDF | Fase 4 |
| 38 | Parcial (Polly ya se usa en Connectors `ProviderCircuitBreaker`, patrón de referencia) | Connectors (referencia, no de onboarding) | Auth.Onboarding (Saga) + todos los servicios de provisioning | Ninguno nuevo | Ninguno | Ninguna nueva (usa `FailureCode` ya cubierto) | Alto | Retry infinito de errores permanentes, o abandono prematuro de transitorios | `FailureClassifier` (§7.1) + Polly 3 intentos + backoff amplio hasta 24h | Fase 17 |
| 39 | No | — | Auth.Onboarding (`OnboardingAdminController`) + Subscription (cancel) + PaymentApp (refund) + Tenant (close) | Ninguno | `OnboardingRefundRequestedIntegrationEvent`, `OnboardingCancelRequestedIntegrationEvent` | Ninguna nueva (reusa agregados existentes con métodos de compensación semántica) | Alto | Refund automático indebido — mitigado por §3.7 (requiere confirmación humana explícita) | 4 endpoints admin (`resume`, `update-and-resume`, `cancel-and-refund`, `force-complete`) | Fase 17 |
| 40 | N/A (meta-resumen, no paso ejecutable) | — | Cross-cutting (documentación) | N/A | N/A | N/A | N/A | N/A | Ya documentado en `01_FlowSpec.md` (tablas a-e) y este documento | N/A |

---

## Resumen de impacto por servicio

| Servicio | Filas con impacto Alto | Filas con impacto Medio | Cambios de código nuevos |
|---|---|---|---|
| Auth (módulo Onboarding) | 6, 7, 12(parcial), 13, 22, 23, 24, 26, 30, 33, 34, 37 | 2, 3, 9, 14, 17, 20, 27, 28, 36 | Alto — nuevo módulo completo (Fases 3-7, 9, 11, 13-15, 17, 18) |
| PaymentApp | 12(parcial), 13 | 8, 11 | Medio — extensión de `SaaSPayment`, sin agregado nuevo (Fase 8) |
| Tenant | 25 | — | Bajo — 1 endpoint M2M + 1 migración (Fase 16) |
| Subscription | 29, 30 | — | Bajo — 1 endpoint M2M + branch + 1 migración (Fase 16) |
| Documents | — | — | Bajo — 1 handler + 1 template + 1 endpoint (Fase 10) |
| Scribe + Notification | — | 16 | Bajo — 2 templates + 2 consumers (Fase 12) |
| CloudStorage | 32(parcial) | — | Nulo — reusa infraestructura existente |
| Postmaster | 15(referencia) | — | Nulo (cero cambios en MVP, per §3.4) |
| Billing | — | — | Nulo (cero cambios, per §3.4) |

Confirma la decisión arquitectónica del plan (§3, §5): el 80% del riesgo/impacto se concentra en Auth (host del bounded context Onboarding), el resto de los servicios reciben cambios quirúrgicos y acotados.

---

## Referencias

- `00_Baseline.md` — snapshot del monorepo antes de esta fase.
- `01_FlowSpec.md` — los 40 pasos, 12 estados, 8 steps, 8 UoW, matriz de fallos y huecos del PDF.
- `PayFlow_Implementation_Plan.md` §3-8 y Fases 3-19 — decisiones arquitectónicas y prompts de implementación detrás de cada celda "Decisión" de esta matriz.
